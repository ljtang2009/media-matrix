import { createApp } from 'vue';
import App from './App.vue';

const app = createApp(App);

app.mount('#app');

// const s = 1;
// function func(arg: string) {
//   // console.log(arg);
//   throw arg;
// }
// func(s);
